#!/bin/sh
# VETRA OS policy: never merge into 'main' locally.
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
	echo "ERROR: Local merges into 'main' are forbidden by policy."
	echo "Push your feature branch and merge it through a Pull Request on GitHub."
	exit 1
fi
exit 0
